# Frontend (React + Tailwind)

## Setup
```bash
cd frontend
npm install
```

## Env
Create `.env` inside `/frontend` if needed (or rely on backend CORS). This project uses a simple backend URL in code.

## Run
```bash
npm run dev -- --port 5173
```

